@model EstacionamentoMvc.Models.MovimentoEstacionamento

@{
    ViewData["Title"] = "Nova Entrada de Veículo";
}

<h2 class="mb-3">🚗 Nova Entrada</h2>

<form asp-action="Entrada" method="post">
    <div class="mb-3">
        <label asp-for="Placa" class="form-label">Placa</label>
        <input asp-for="Placa" class="form-control" placeholder="ABC-1234" />
        <span asp-validation-for="Placa" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Modelo" class="form-label">Modelo</label>
        <select asp-for="Modelo" asp-items="ViewBag.Modelos" class="form-select">
            <option value="">-- selecione o modelo --</option>
        </select>
        <span asp-validation-for="Modelo" class="text-danger"></span>
    </div>

    <div class="row mb-3">
        <div class="col-md-6">
            <label class="form-label">Tarifa Inicial</label>
            <input type="text" class="form-control" value="@ViewBag.TarifaInicial" readonly />
        </div>
        <div class="col-md-6">
            <label class="form-label">Tarifa por Hora</label>
            <input type="text" class="form-control" value="@ViewBag.TarifaHora" readonly />
        </div>
    </div>

    <button type="submit" class="btn btn-success">
        ➕ Registrar Entrada
    </button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
